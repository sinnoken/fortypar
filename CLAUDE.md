# FortiToolkit — 原則指引

## 鐵則（違反即產出錯誤 CLI 或誤判合規）

1. **CLI 註解永不接在指令後面** — FortiOS 把指令之後的一切當參數。所有產生的註解一律自成一行，不得「整理」成行尾註解。
2. **引用分析不得引入欄位白名單** — 掃描每個物件的每個值，任何 token 命中已知物件名即算引用。白名單漏一個欄位就會靜默地把在用的物件放進刪除清單。root 是推導出來的（不在 WatchSet 者即為 root），不是列舉的。
3. **合規規則跑物件樹，不跑文字** — 文字規則的萬用字元會越過 `next` 走進下一個物件，造成假通過。結構比對一次只看得到一個物件。匯入的外部文字規則只做 review、不執行。
4. **缺鍵 ≠ 未設定** — FortiOS 匯出時省略原廠預設值。每條斷言須宣告 `Default`，取值時鍵不存在則代入它，並在細節中標成 `(default)`。新增規則前先確認原廠預設值。
5. **內建物件永不進刪除清單** — 內建判定是硬停，獨立於引用分析之外。它們合法地零引用但刪不得，改列為「held back」。
6. **刪除前的 refcnt 驗證步驟不可從產出中拿掉** — config 檔看不到 FortiManager、SDN connector、裝置記憶體裡的引用。**裝置才是權威，檔案不是。**
7. **重複值的刪除一律註解掉** — 合併重複需先把每一處引用改指到 keeper，不是批次操作。
8. **刪除段落順序：群組先於成員** — 順序反了，刪成員時群組還指著它，FortiOS 回 `-23`。
9. **改完任一 `.ps1` 後，用絕對路徑確認產出目錄的成品**；純文件不需要。

***

## 設計原則（改 code 前自我檢查「有沒有違反 X？」）

* **SoC + 單向依賴** — 依賴方向 `GUI → Rules/Cli → Core → 資料`。Core 內不得反向引用任何控制項。UI 附加檔只讀共享狀態，不寫。
* **Fail-safe direction** — 判斷任何取捨時先問：這個失敗會朝哪個方向倒？「誤報一條要人工看」永遠優於「漏報一條讓人刪掉在用的物件」。
* **SSOT** — 名單、色票、排序權重等常數各自只定義一次。
* **No Magic Numbers** — 數字收進具名結構。
* **Determinism** — 同一份輸入兩次分析結果必須相同。排序一律用預先算好的字串鍵，不靠 hashtable 列舉順序。
* **Precomputed sort key** — 熱路徑的排序不在 `Sort-Object` 運算式裡現算；建模時算一次、排序時只讀欄位。
* **Lazy evaluation** — 重量級分頁只在使用者切過去時才建模並快取；載入流程不需要知道它存在。新增分頁沿用此模式。
* **Immutability** — 分析函式不 mutate 輸入物件；補欄位只允許在物件剛產生的同一函式內。
* **Graceful degradation** — 缺少對應 section 時標記為 skip 而非 fail，統計上獨立為「Not applicable」。**「沒有這個東西」不等於「這個東西不合規」。**
* **可追溯到原始碼行** — 每個物件帶行號，細節視圖據此印出原文前後文。**新增任何發現類型都要帶行號**，否則使用者無從查證。
* **Desktop WinForms，非響應式** — 版面靠 `Dock` + `SplitContainer` + 手算座標。不要引入自動佈局容器改寫既有版面，混用會讓手算座標失準。

***

## PowerShell 慣例

* **`$null` 放比較式左邊** — 右邊放 `$null` 且左邊為陣列時，`-eq` 變成過濾運算而非布林判斷。
* **`[void]` 的使用時機** — `ArrayList.Add()` / `StringBuilder.Append*()` / `DataTable.Rows.Add()` / `TabPages.Add()` 需要；泛型 `List[object]` 的 `Add()` 本身回 void，不要加。
* **集合一律用泛型 `List[object]`** — 不用 `@()` 累加，`+=` 每次重建整個陣列。
* **hashtable 查找取代巢狀迴圈** — 熱路徑用巢狀 hashtable（少配置一個字串鍵物件），非熱路徑用內插鍵可讀性較好；兩種並存是刻意的。
* **函式動詞** — 既有非核准動詞現況接受，但新增函式一律用核准動詞（`Get-` / `Set-` / `New-` / `Test-` / `Update-` / `Remove-` / `Invoke-` / `Build-`），別擴大例外。
* **`$script:` 前綴表示跨函式共享狀態** — 狀態變數與各種註冊表加前綴；控制項變數不加（建立後只讀不換）。
* **路徑一律用主檔既有的 `$here`，不用 `$PSScriptRoot`**（已含 dot-source / ISE fallback）。
* **`-LiteralPath`** — 檔案操作一律用它，檔名可能含 `[` `]`。
* **編碼** — 讀檔用 `ReadAllLines()`；寫檔用 `WriteAllText` 搭帶 BOM 的 UTF8，Excel 開 CSV 才不會亂碼。

## WinForms 慣例

* **勾選狀態的批次變更一律包 suspend 旗標**，事件處理開頭檢查它直接 return；否則批次勾選會觸發等量次數的重建。
* **DataGridView 的 checkbox 需要 `CurrentCellDirtyStateChanged` + `CommitEdit`**，否則要等焦點離開該格才寫進 DataTable。新增 grid 別漏。
* **欄位以名稱存取，不以索引**。既有的索引硬編是技術債，別擴散。
* **篩選用 `DataView.RowFilter`，不重建 DataTable**；字串值要 `.Replace("'", "''")` 逃逸。
* **`ShowDialog()` / 剪貼簿是 STA 依賴** — PowerShell 7 預設 MTA，需 `-STA`，並保留 try/catch 降級路徑。
* **控制項加入順序決定 Dock 疊放** — 後加入的 `Dock='Top'` 排在先加入者之上，既有加入順序是刻意的，別重排。
* **文字輸出到多行 TextBox 前要把 LF 轉 CRLF**，否則擠成一行。

***

## 規則包慣例

* 引擎支援 `each` / `section` / `exists` / `absent` 四種 Mode；未使用的 Mode 是引擎能力，保留。
* 新增規則優先用既有 `Op`。需要新 Op 時**同步改條件判定與失敗細節兩處**——漏改後者會讓失敗訊息退化成沒有上下文的 `key = value`。
* **每條規則必須有 `Why`**，寫「不做會怎樣」而非重述規則本身，會原樣顯示給使用者。
* `Zone` 三值：`global` / `vdom` / `any`。
* `Fix` 裡需要站點特定值的行以 `#` 開頭，產出時原樣輸出，使用者會直接貼進裝置。

## 命名原則

* **大小寫傳達可變性** — 不可變設定用 ALL\_CAPS 或 PascalCase 註冊表；運行期狀態用 camelCase。
* **短名限縮在單一函式或單一檔案內**（熱路徑的單字母欄位）；**跨檔案的公開結構請用完整名稱**。
* **名稱說明意圖，不說明型別**。
* **函式名以動詞開頭**，說明做什麼而非回傳什麼。

## 文件慣例

* **不寫死可變數字** — 用結構或量級表述。
* 效能說明標明**量級與前提**，別寫成絕對秒數。
